{% extends 'admin/base.html.twig' %}

{% block title %}Create New Room{% endblock %}

{% block body %}
    <div class="flex min-h-full flex-col justify-center px-6 py-12 lg:px-8">
        <div class="sm:mx-auto sm:w-full sm:max-w-sm">
            <h2 class="mt-10 text-center text-2xl font-bold leading-9 tracking-tight text-gray-900">Create a new Room</h2>
        </div>

        <div class="mt-10 sm:mx-auto sm:w-full sm:max-w-sm">
            {{ include('room/_form.html.twig', {'button_label': 'Create Room'}) }}

            <p class="mt-10 text-center text-sm text-gray-500">
                <a href="{{ path('app_room_index') }}" class="font-semibold leading-6 text-blue-500 hover:text-blue-400">Back to list</a>
            </p>
        </div>
    </div>
{% endblock %}
