<div class="min-h-full">
    <nav class="bg-gray-800">
        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <div class="flex h-16 items-center justify-between">
                <div class="flex items-center">
                    <div class="flex-shrink-0">
                        <a href="{{ path('app_home') }}">
                            <img class="h-8 w-8" src="{{ asset('images/chatstorylogo.png') }}" alt="Your Company">
                        </a>
                    </div>
                    <div class="hidden md:block">
                        <div class="ml-10 flex items-baseline space-x-4">
                            <a href="{{ path('app_home') }}"
                               class="rounded-md px-3 py-2 text-sm font-medium {{ app.request.attributes.get('_route') == 'app_home' ? 'bg-gray-900 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white' }}"
                               aria-current="{{ app.request.attributes.get('_route') == 'app_home' ? 'page' : '' }}">
                                Home
                            </a>
                            <a href="{{ path('app_home') }}"
                               class="rounded-md px-3 py-2 text-sm font-medium {{ app.request.attributes.get('_route') == 'app_rooms' ? 'bg-gray-900 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white' }}"
                               aria-current="{{ app.request.attributes.get('_route') == 'app_rooms' ? 'page' : '' }}">
                                Rooms
                            </a>
                        </div>
                    </div>
                </div>
                <div class="hidden md:block">
                    <div class="ml-4 flex items-center md:ml-6">
                        <div class="relative ml-3">
                            {% if app.user %}
                                <div class="flex gap-3">
                                    <a href="{{ path('app_logout') }}" class="rounded-md px-3 py-2 text-sm font-medium text-gray-300 hover:bg-gray-700 hover:text-white">Logout</a>
                                    <a href="{{ path('user_profile_edit') }}" type="button" class="relative flex max-w-xs items-center rounded-full bg-gray-800 text-sm focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-800 h-fit" id="user-menu-button" aria-expanded="false" aria-haspopup="true">
                                        <img class="h-8 w-8 rounded-full" src="{{ app.user.image ? asset('uploads/profile_pictures/' ~ app.user.image) : asset('images/default_profile.png') }}" alt="{{ app.user.username }}">
                                    </a>
                                </div>
                            {% else %}
                                <div class="flex gap-3">
                                    <a href="{{ path('app_login') }}" class="rounded-md px-3 py-2 text-sm font-medium text-gray-300 hover:bg-gray-700 hover:text-white">Login</a>
                                    <a href="{{ path('app_register') }}" class="rounded-md px-3 py-2 text-sm font-medium text-gray-300 hover:bg-gray-700 hover:text-white">Register</a>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                </div>
                <div class="-mr-2 flex md:hidden">
                    <button type="button" class="relative inline-flex items-center justify-center rounded-md bg-gray-800 p-2 text-gray-400 hover:bg-gray-700 hover:text-white focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-800" aria-controls="mobile-menu" aria-expanded="false">
                        <span class="absolute -inset-0.5"></span>
                        <span class="sr-only">Open main menu</span>
                        <svg class="block h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true" data-slot="icon">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5"></path>
                        </svg>
                    </button>
                </div>
            </div>
        </div>

        <div class="md:hidden" id="mobile-menu">
            <div class="space-y-1 px-2 pb-3 pt-2 sm:px-3">
                <a href="{{ path('app_home') }}"
                   class="block rounded-md px-3 py-2 text-base font-medium {{ app.request.attributes.get('_route') == 'app_home' ? 'bg-gray-900 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white' }}"
                   aria-current="{{ app.request.attributes.get('_route') == 'app_home' ? 'page' : '' }}">
                    Home
                </a>
                <a href="{{ path('app_home') }}"
                   class="block rounded-md px-3 py-2 text-base font-medium {{ app.request.attributes.get('_route') == 'app_rooms' ? 'bg-gray-900 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white' }}"
                   aria-current="{{ app.request.attributes.get('_route') == 'app_rooms' ? 'page' : '' }}">
                    Rooms
                </a>
                {% if app.user %}
                    <a href="{{ path('app_logout') }}" class="block rounded-md px-3 py-2 text-base font-medium text-gray-300 hover:bg-gray-700 hover:text-white">Logout</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="block rounded-md px-3 py-2 text-base font-medium text-gray-300 hover:bg-gray-700 hover:text-white">Login</a>
                    <a href="{{ path('app_register') }}" class="block rounded-md px-3 py-2 text-base font-medium text-gray-300 hover:bg-gray-700 hover:text-white">Register</a>
                {% endif %}
            </div>
        </div>
    </nav>
</div>
